timestamps {
    node {
        stage("start") {
            echo "Starting"
        }

        stage("middle") {
            echo "Cooking"
        }

        stage("end") {
            echo "Finishing"
        }
    }
}