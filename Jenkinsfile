properties([overrideIndexTriggers(true)])

timestamps {
    node {
        stage("start") {
            echo "Starting"
        }

        stage("middle") {
            echo "Steaming"
        }

        stage("end") {
            echo "Finishing"
        }
    }
}