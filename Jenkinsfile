timestamps {
    node {
        stage("start") {
            echo "Starting"
        }

        stage("middle") {
            echo "Marinating"
        }

        stage("end") {
            echo "Finishing"
        }
    }
}