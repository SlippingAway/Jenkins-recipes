timestamps {
    node {
        stage("start") {
            echo "Starting"
        }

        stage("middle") {
            echo "Marinating just a bit"
        }

        stage("end") {
            echo "Finishing"
        }
    }
}