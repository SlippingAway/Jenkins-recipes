properties([overrideIndexTriggers(alwaysBuild)])

timestamps {
    node {
        stage("start") {
            echo "Starting"
        }

        stage("middle") {
            echo "Grilling"
        }

        stage("end") {
            echo "Finishing"
        }
    }
}